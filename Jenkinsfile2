pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                timeout(time: 2, unit: 'MINUTES') {
                    retry(2) {
                        sh '/home/dell/flakey-deploy.sh'
                    }
                }
            }
        }
    }
}

t
