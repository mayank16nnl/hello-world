version: 2.1
workflows:
 test-deploy:
   jobs:
     --test
     -deploy
jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01 # recommended linux image
    resource_class: medium
    steps:
       - run: echo 'Hello, Ubuntu'
         

      
 
